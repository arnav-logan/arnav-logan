================================================================================
1. In emacs, most commands use a prefix argument as a repeat count.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
2. In emacs, to kill the next word after the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  C-d
	B.  M-e
	C.  M-d
	D.  M-DEL

================================================================================
3. In emacs, the text within parentheses on the mode line indicates the editing mode.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. In emacs, the screen can be split horizontally with the key sequence:
------------------------------------------------------------
	A.  C-x 0 (the second character is the digit zero)
	B.  C-x 1
	C.  C-x 3
	D.  C-x 2

================================================================================
5. In emacs, the screen can be split vertically with the key sequence:
------------------------------------------------------------
	A.  C-x 2
	B.  C-x 0 (the second character is the digit zero)
	C.  C-x 3
	D.  C-x 1

================================================================================
6. In emacs, to move the cursor to the end of the line, the correct key sequence is:
------------------------------------------------------------
	A.  C-e
	B.  M-e
	C.  C-a
	D.  M-a

================================================================================
7. In emacs, to return to basic one-window editing, the correct key sequence is:
------------------------------------------------------------
	A.  ESC 1 (the second character is the digit one)
	B.  C-x 1 (the second character is the digit one)
	C.  M-x 1 (the second character is the digit one)
	D.  ESC ESC

================================================================================
8. In emacs, to move the cursor to the end of the file, the correct key sequence is:
------------------------------------------------------------
	A.  C-b
	B.  C-e
	C.  ESC-<
	D.  ESC->

================================================================================
9. In emacs, the search command is incremental.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
10. Emacs can only edit one file at any given time.
------------------------------------------------------------
	A.  True
	B.  False

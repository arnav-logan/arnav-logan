================================================================================
1. What symbol is used to represent the user's home directory?
------------------------------------------------------------
	A.  / (a slash or forward slash)
	B.  ~ (a tilde)
	C.  \ ( a backslash)
	D.  $ (a dollar sign)

================================================================================
2. The command used to create a new directory is:
------------------------------------------------------------
	A.  mkdir
	B.  cwd
	C.  createdir
	D.  cd

================================================================================
3. The client-server model is antiquated and is no longer commonly used.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. A client-server model is a means of distributing resources between the provider of a service and a consumer of that service, the client.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
5. Unix integrates all files into a single directory structure.
------------------------------------------------------------
	A.  True
	B.  False

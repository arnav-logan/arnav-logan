================================================================================
1. In emacs, to kill a segment of text that has already been selected, the correct key sequence is:
------------------------------------------------------------
	A.  M-k
	B.  M-w
	C.  C-w
	D.  C-k

================================================================================
2. In emacs, to end a partially entered command, the correct key sequence is:
------------------------------------------------------------
	A.  C-h
	B.  M-g
	C.  C-g
	D.  M-h

================================================================================
3. In emacs, many of the commands that start with the key sequence "C-y" deal with windows, files, and buffers.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. To start the tutorial in emacs, the correct key sequence is:
------------------------------------------------------------
	A.  TAB-h t
	B.  SHIFT-h t
	C.  META-h t
	D.  CTRL-h t

================================================================================
5. In emacs, to move the cursor to the end of a sentence, the correct key sequence is:
------------------------------------------------------------
	A.  C-f
	B.  C-e
	C.  M-e
	D.  M-f

================================================================================
6. In emacs, an incremental forward search may be initiated with the key sequence:
------------------------------------------------------------
	A.  C-x C-f
	B.  M-x forward-search
	C.  M-s
	D.  C-s

================================================================================
7. In emacs, the location of the cursor in the text is also called "point".
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
8. In emacs, the asterisks (stars) at the front of the mode line indicate:
------------------------------------------------------------
	A.  Changes have been made to the buffer that have not yet been saved to the file.
	B.  Emacs is busy in a CPU intensive task.
	C.  No changes have been made to the buffer that have not already been saved to the file.
	D.  Emacs is busy accessing the disk.

================================================================================
9. In emacs, the cursor can be moved to another window with the key sequence:
------------------------------------------------------------
	A.  C-x 0 (the second character is the digit zero)
	B.  C-x 1
	C.  C-x 2
	D.  C-x o (the second character is a lowercase "O")

================================================================================
10. In emacs, the difference between "killing" and "deleting" text is that "deleted" text can be reinserted at any position, whereas "killed" text cannot be reinserted in this way.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
11. In emacs, in order to retrieve text from an earlier kill, the correct key sequence is:
------------------------------------------------------------
	A.  M-y
	B.  M-INS
	C.  C-SPC
	D.  C-y

================================================================================
12. In emacs, in order to find a file, the correct key sequence is:
------------------------------------------------------------
	A.  C-s
	B.  C-f
	C.  M-f
	D.  C-x C-f

================================================================================
13. In emacs, it is an error to find a file which does not yet exist.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
14. In emacs, reinsertion of text is called "yanking".
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
15. In emacs, to move the cursor to the beginning of the line, the correct key sequence is:
------------------------------------------------------------
	A.  C-a
	B.  M-e
	C.  M-a
	D.  C-e

================================================================================
16. In emacs, there's no need to explicitly save the contents you are editing to a file.  Emacs performs this automatically as you type.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
17. In emacs, to move the cursor down a single line, the correct key sequence is:
------------------------------------------------------------
	A.  M-d
	B.  C-d
	C.  M-n
	D.  C-n

================================================================================
18. In emacs, most commands use a prefix argument as a repeat count.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
19. In emacs, an incremental reverse search may be initiated with the key sequence:
------------------------------------------------------------
	A.  C-x C-r
	B.  M-r
	C.  C-r
	D.  M-x reverse-search

================================================================================
20. In emacs, minor modes enable minor modifications to the major mode.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
21. In emacs, to kill the next word after the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  M-DEL
	B.  M-d
	C.  M-e
	D.  C-d

================================================================================
22. To end an emacs session, the correct key sequence is:
------------------------------------------------------------
	A.  C-x C-c
	B.  M-x M-c
	C.  C-c C-x
	D.  M-c M-x

================================================================================
23. In emacs, a newline character can be deleted just like any other character.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
24. In a text file, the special character which separates one line from the next is called a:
------------------------------------------------------------
	A.  Newline character
	B.  Line separation character
	C.  Line divider character
	D.  Nextline character

================================================================================
25. Emacs can only edit one file at any given time.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
26. In emacs, the screen can be split horizontally with the key sequence:
------------------------------------------------------------
	A.  C-x 2
	B.  C-x 0 (the second character is the digit zero)
	C.  C-x 1
	D.  C-x 3

================================================================================
27. In emacs, in order to save multiple files, the correct key sequence is:
------------------------------------------------------------
	A.  M-x s
	B.  C-x s
	C.  M-s
	D.  C-x C-s

================================================================================
28. When using emacs in a text terminal, long text lines which are continued on to another screen line are indicated by which character on the rightmost screen column?
------------------------------------------------------------
	A.  / (a forward slash)
	B.  $ (a dollar sign)
	C.  ! (an exclamation point)
	D.  \ (a backslash)

================================================================================
29. In emacs, to move the cursor backward a single character, the correct key sequence is:
------------------------------------------------------------
	A.  C-b
	B.  C-p
	C.  M-p
	D.  M-b

================================================================================
30. In emacs, the text within parentheses on the mode line indicates the editing mode.
------------------------------------------------------------
	A.  True
	B.  False
